# CS3339---HW3---Cache-Memory-Simulator

This program reads memory access values from input, then maps the access of each
to a cache set. It checks if the tag is already in a set. If it's found, it's a 
hit, if not, it's a miss and the cache is updated. Then it prints a result for 
each access. 
